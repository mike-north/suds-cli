## API Report File for "@boba-cli/timer"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Cmd } from '@boba-cli/tea';
import { Model } from '@boba-cli/tea';
import { Msg } from '@boba-cli/tea';

// @public
export class StartStopMsg {
    constructor(
    id: number,
    running: boolean);
    readonly id: number;
    readonly running: boolean;
    // (undocumented)
    readonly _tag = "timer-start-stop";
}

// @public
export class TickMsg {
    constructor(
    id: number,
    tag: number,
    timeout: boolean);
    readonly id: number;
    readonly tag: number;
    // (undocumented)
    readonly _tag = "timer-tick";
    readonly timeout: boolean;
}

// @public
export class TimeoutMsg {
    constructor(id: number);
    // (undocumented)
    readonly id: number;
    // (undocumented)
    readonly _tag = "timer-timeout";
}

// @public
export class TimerModel implements Model<TimerMsg, TimerModel> {
    id(): number;
    init(): Cmd<TimerMsg>;
    // (undocumented)
    readonly interval: number;
    static new(options: TimerOptions): TimerModel;
    running(): boolean;
    start(): Cmd<TimerMsg>;
    stop(): Cmd<TimerMsg>;
    timedOut(): boolean;
    // (undocumented)
    readonly timeout: number;
    toggle(): Cmd<TimerMsg>;
    update(msg: Msg): [TimerModel, Cmd<TimerMsg>];
    view(): string;
    static withInterval(timeout: number, interval: number): TimerModel;
}

// @public
export type TimerMsg = TickMsg | TimeoutMsg | StartStopMsg;

// @public
export interface TimerOptions {
    interval?: number;
    timeout: number;
}

// (No @packageDocumentation comment for this package)

```
