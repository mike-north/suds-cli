{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    // Enable emit so the demo-website can import and run compiled examples
    "noEmit": false,
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "rootDir": "./src",
    "outDir": "./dist",
    "declaration": true,
    "declarationMap": true
  },
  "include": ["src/**/*.ts"]
}
