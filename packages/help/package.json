{
  "name": "@suds-cli/help",
  "description": "Help components for Suds terminal UIs: key binding renderer and scrollable help bubble",
  "version": "0.1.0-alpha.0",
  "dependencies": {
    "@suds-cli/chapstick": "0.1.0-alpha.0",
    "@suds-cli/key": "0.0.0",
    "@suds-cli/tea": "0.0.0",
    "@suds-cli/viewport": "0.0.1-alpha.0"
  },
  "devDependencies": {
    "typescript": "5.8.2",
    "vitest": "^4.0.16"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "files": [
    "dist"
  ],
  "main": "./dist/index.js",
  "scripts": {
    "build": "pnpm run clean && tsc -p ./tsconfig.json",
    "check:api-report": "pnpm run generate:api-report",
    "check:eslint": "pnpm run lint",
    "clean": "rm -rf dist",
    "generate:api-report": "api-extractor run --local",
    "lint": "eslint \"{src,test}/**/*.{ts,tsx}\"",
    "test": "vitest run"
  },
  "type": "module",
  "types": "./dist/index.d.ts"
}
